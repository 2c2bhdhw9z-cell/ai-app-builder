// This lives under node_modules and must be excluded by PATH — a dependency's own copy of a
// telemetry SDK is not YOUR coupling. The EXCLUDES filter (path-only) must drop it.
import '@sentry/browser';
export const collectUrl = 'https://x.example.com/collect';
